<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="medico.list">
      <field name="name">medico list</field>
      <field name="model">medico.medico</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="medico.action_window">
      <field name="name">medico window</field>
      <field name="res_model">medico.medico</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="medico.action_server">
      <field name="name">medico server</field>
      <field name="model_id" ref="model_medico_medico"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="medico" id="medico.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="medico.menu_1" parent="medico.menu_root"/>
    <menuitem name="Menu 2" id="medico.menu_2" parent="medico.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="medico.menu_1_list" parent="medico.menu_1"
              action="medico.action_window"/>
    <menuitem name="Server to list" id="medico" parent="medico.menu_2"
              action="medico.action_server"/>
-->
  </data>
</odoo>
